A mini Docker-like container system built in Python with support for layered builds, caching, and runtime isolation using chroot.

Instructions:
1. Run `sudo python3 setup_base.py` to download the Alpine Linux minirootfs and initialize the real base image.
2. Check `Commands.txt` to run the demonstration.
